#!/bin/bash

# apt-get update
# apt-get install -y docker.io
sudo snap refresh
sudo snap install docker
sudo snap refresh
sudo snap install microk8s --classic
sudo snap install kubectl --classic
microk8s status --wait-ready
microk8s enable dashboard dns registry istio
sudo usermod -a -G microk8s ubuntu

#  sftp -i microk8s-instance-key.pem ubuntu@ec2-54-229-245-206.eu-west-1.compute.amazonaws.com
#  put -r ./docker-api

#  sudo docker build ./docker-api -t api

#  df -h

# sudo docker run --name api -p 3000:3000 -d api
# sudo docker container ls -a



# sudo snap refresh
# sudo snap install microk8s --classic

# sudo snap install kubectl --classic

# microk8s.inspect
# microk8s.status
# microk8s.stop
# microk8s.start

# microk8s kubectl cluster-info